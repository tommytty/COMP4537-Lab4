const dbCredentials = {
  host: 'localhost', // or your host IP
  database: 'dbserver',
  adminUser: 'admin',
  adminPass: 'admin',
  readerUser: 'reader',
  readerPass: 'reader',
};

module.exports = dbCredentials;
